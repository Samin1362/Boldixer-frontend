# Absolute origin of the deployment. Metadata, canonical URLs, OG images and
# sitemap.xml all read this; without it they fall back to http://localhost:3000,
# which breaks link previews in production.
#
# On Vercel: Project → Settings → Environment Variables → add for Production,
# Preview and Development.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
